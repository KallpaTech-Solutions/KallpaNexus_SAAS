import { LandingPage } from "@/components/marketing/landing-page";

/** Raíz `/`: web pública de Kallpa Nexus. El panel operativo está en `/dashboard`. */
export function HomeEntry() {
  return <LandingPage />;
}
