namespace KallpaNexus.Domain.Tenancy;

/// <summary>
/// Códigos estables de roles semilla (la definición real y permisos viven en BD).
/// </summary>
public enum RolPlataformaCodigo
{
    SuperAdmin = 100,
    AdminPlataforma = 80,
    GerentePlataforma = 50
}
